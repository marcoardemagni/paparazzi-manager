/**
 * Trascrizione audio con OpenAI Whisper — v2
 * Per ora restituisce null (funzione non attiva).
 * Implementazione completa nella prossima iterazione.
 */
export async function transcribeAudio(mediaUrl) {
  // TODO v2: scarica .ogg da Twilio, invia a OpenAI Whisper API
  // const OpenAI = await import("openai");
  // const client = new OpenAI.default({ apiKey: process.env.OPENAI_API_KEY });
  // ...
  console.log("Whisper non ancora attivo. MediaUrl:", mediaUrl);
  return null;
}
